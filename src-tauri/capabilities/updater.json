{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "In-app updates (only present in builds with the `updater` Cargo feature)",
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default"
  ]
}
